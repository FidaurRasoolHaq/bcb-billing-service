NODE_ENV=development
PORT=3000

# Fee charged in GBP for every transaction in a billing period that exceeds
# an account's transactionThreshold. Not part of the spec's request payloads,
# so it is centrally configurable here instead of being hardcoded.
TRANSACTION_FEE_GBP=0.20
